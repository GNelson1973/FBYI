<% if !can? :create, Post %>
<div class="guest_home">
  <div class="guest_msg1">Hello stranger<div>
  <div class="guest_msg2">Don't be shy like me<div>
  <div class="guest_msg3">Share your fashion style!<div>
  <div class="guest_msg4">Sign up now<div>
</div>
<% end %>

<% if can? :create, Post %>
<main class="container col-md-10 col-md-offset-3">
  <%= render "form" %>
  <br>
  <%= render @posts %>
</main>
<% end %>
